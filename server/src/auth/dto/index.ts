// Here we are using a barrel export pattern to export all the fields
// The advantage is that it then allows you to import all dtos at once
// rather than doing it individually for each from their respective paths
export * from "./auth.dto";
